# Private local config (never commit filled copies)
config/local.json
config/local.yaml

# Deploy manifests — system-side only (generated by install/sync)
.deploy/system-manifest.json
.deploy/last-sync.json

# Session audit trail (optional tooling)
.audit.log

# Tooling
.DS_Store
__pycache__/
*.pyc

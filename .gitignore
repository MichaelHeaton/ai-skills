# Private local config (never commit filled copies)
config/local.json
config/local.yaml

# Deploy manifests — system-side only (generated by install/sync)
.deploy/system-manifest.json
.deploy/last-sync.json

# Desktop/claude.ai packaging build output (local, not tracked state)
.deploy/packages/

# Session audit trail (optional tooling)
.audit.log

# Tooling
.DS_Store
__pycache__/
*.pyc

# Claude Code agent worktrees — local, per-session scratch checkouts
.claude/worktrees/

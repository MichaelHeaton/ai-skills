# DEPRECATED — use config/accounts.shell.template instead.
# Copy: cp config/accounts.shell.template ~/.config/ai-skills/accounts.shell
# Note: *.env paths are blocked by Claude's block-env-read hook.
